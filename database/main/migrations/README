# main 数据库迁移

本目录保存 main 数据库的 Alembic 环境和版本脚本。数据库连接来自项目环境变量中的 `DB_CONNECTIONS__MAIN__*`，不会从 `alembic.ini` 读取数据库 URL。

在项目根目录执行：

```shell
uv run alembic -c database/main/alembic.ini current
uv run alembic -c database/main/alembic.ini revision --autogenerate -m "migration message"
uv run alembic -c database/main/alembic.ini upgrade head
uv run alembic -c database/main/alembic.ini downgrade -1
```

自动生成的迁移是候选结果，提交前必须人工检查 `upgrade()` 和 `downgrade()`。

新增 main 数据库 ORM 模型时，必须同时将模型模块加入迁移环境的模型加载入口，使表结构注册到 `MainBase.metadata` 后再运行自动生成。
