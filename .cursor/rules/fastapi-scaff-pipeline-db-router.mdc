---
description: 调用链、DB 模块、Router 约束
alwaysApply: true
---

# 调用链（必须遵守）

```text
API → Application → Infrastructure
```

## 禁止

- Endpoint / Router **直接**操作 DB、Session、ORM
- Endpoint / Router 禁止声明 `Depends(get_db_session)` 作为业务路径注入；会话获取与事务边界由 Application Service / UoW 统一管理
- Repository 内自行 `commit/rollback`（事务提交与回滚必须由 Application Service / UoW 统一控制）
- Middleware 内直接返回业务错误响应（中间件应抛异常，统一交给全局 exception handler）

## Session 生命周期（Lazy Session）

- **Application 层按需开会话**：仅在真正需要访问 Repository 前，才创建 Session（`async with ...`）。
- **Infrastructure 层只提供能力**：`infrastructure/db/session.py` 提供 `sessionmaker` / provider，不承担业务事务编排。
- **Repository 只做数据访问**：不在 Repository 内 `commit/rollback`，由 Application 统一控制事务边界。
- **接口层保持薄**：Endpoint 只收参数与返回 DTO，不感知 `AsyncSession`。

## `infrastructure/db/` 拆分

| 文件 | 作用 |
|------|------|
| `engine.py` | 创建连接 |
| `session.py` | Session 管理 |
| `base.py` | ORM 基类 |
| `models/` | 表模型 |

目的：避免循环依赖、便于多库与测试替换、配合 Alembic。

## Router 聚合顺序

1. `endpoints/*.py` — 定义路由处理函数（薄层）
2. `v1/router.py` — 聚合模块
3. `api/router.py` — 聚合版本
4. `main.py` — 挂载应用

路径示例：`/api/v1/...`

## RESTful 路由与 CRUD 方法命名

- 资源路径使用**复数**名词（如 `users`），版本前缀下形如 `/api/v1/{resources}`；**禁止**在路径中塞动词（如 `/getUser`、`/userCreate`）。
- 单资源使用路径参数标识（如 `/{user_id}`），**禁止**用 query 代替资源标识表达「删某条」等语义（删除仍用 `DELETE .../{id}`）。
- 标准资源 CRUD 与 **HTTP 动词、Endpoint 处理函数名、Application Service 方法名**对齐如下（仓储层对外方法名亦建议一致，便于检索）：

| 语义 | HTTP | 路径示例 | 方法名 |
|------|------|----------|--------|
| 新增 | POST | `/api/v1/users` | `store` |
| 详情 | GET | `/api/v1/users/{id}` | `show` |
| 更新 | PATCH（或 PUT） | `/api/v1/users/{id}` | `update` |
| 删除 | DELETE | `/api/v1/users/{id}` | `destroy` |

- 列表分页（`index`）等另行约定时，保持资源复数与查询参数分工（如 `?page=`），勿破坏资源名一致性。
- 非标准 CRUD 的业务动作（如状态流转）方法命名采用**动词+名词**：`verb_noun`（如 `update_status`）；Endpoint / Service / Repository 建议保持同名，便于检索与链路追踪。

## 架构口诀

接口层只接请求；应用层做流程；领域层管规则；基础设施做实现。
