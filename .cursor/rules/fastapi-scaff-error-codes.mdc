---
description: 错误码 ErrorCodeBuilder、SERVICE_CODE、Biz/System 异常与全局 handler
alwaysApply: true
---

# 错误码与异常

## 十位结构

`full = HTTP×10⁷ + SVC×10⁴ + PART`，其中 `PART = BB×100 + CC`（用 `ErrorCodeBuilder.compose_partial(bb, cc)`）。

| 位段 | 含义 |
|------|------|
| 第 1～3 位 | **HTTP 状态码**（如 200、400、404、422、500；构造时须为 100～599） |
| 第 4～6 位 | **服务码**，仅来自环境变量 **`SERVICE_CODE`**（`config.service`，两位或三位数字，运行时规范为三位），**不要**写进 `IntEnum` |
| 第 7～8 位 | **模块** BB（00～99） |
| 第 9～10 位 | **具体错误** CC（00～99） |

- **Enum**：只存 **PART**（0～9999），即模块与具体两段的合成；**禁止**手写完整十位或手拼各段。
- **HTTP 与消息**：由各枚举的 **`status_code()`** / **`message()`** 提供（与 REST 语义一致）。
- **唯一出口**：完整码只通过 **`ErrorCodeBuilder.build(http_status, partial)`**（`get_error_code_builder()`）得到；展示 **`f"{full:010d}"`**。

## 异常（必须）

- **业务规则失败**：`raise BizException(业务 errors IntEnum, message)`
- **系统 / 第三方 / 基础设施**：`raise SystemException(对应 IntEnum, message)`（仍通过枚举的 `status_code()` 决定前三位 HTTP）
- **全局**：`register_exception_handlers(app)`；未捕获异常走兜底 handler，勿在接口层吞掉上述异常。

## 统一错误出口（新增）

- 项目内错误响应只允许由**全局异常处理器**生成（`register_exception_handlers`）。
- Endpoint / Middleware / Service / Repository / Utils **只抛异常，不处理错误响应**。
- 允许场景仅限：`raise BizException`、`raise SystemException` 或上抛原始异常，由全局 handler 统一映射为响应。

## 放置

- 系统类低位：`app/common/enums/error_code.py`
- 业务低位：`app/application/{module}/errors.py`

## 禁止

手写完整十位、Enum 含服务码、业务不用 BizException、系统不用 SystemException、绕过 handler、字符串当唯一错误码、错误枚举进 infrastructure、在非全局 handler 处构造/返回错误响应。
