---
description: FastAPI + 轻量 DDD — 目录结构与分层职责
alwaysApply: true
---

# 目录与分层

## 设计原则

Clean Architecture + 轻量 DDD。核心：**分层清晰、职责单一、依赖单向、易扩展**。

## 目录树（新代码须对齐）

```text
app/
├── main.py
├── domain/                 # 领域：实体、值对象、领域服务（复杂业务再加重）
├── application/
│   ├── services/           # 用例编排、业务服务
│   └── enums/              # 业务枚举（默认）；进阶可放 domain/{mod}/enums.py
├── interfaces/
│   ├── api/
│   │   ├── router.py
│   │   └── v1/router.py, v1/endpoints/*.py
│   ├── schemas/            # Pydantic DTO
│   └── middleware/
├── infrastructure/
│   ├── db/ (engine, session, base, models/)
│   └── redis/
└── common/ (response, enums 仅跨模块通用, utils, exceptions)
config/
database/migrations/        # Alembic
```

## 分层职责

| 层 | 职责 |
|----|------|
| **domain** | 核心业务规则 |
| **application** | 用例编排；调 repository / service；**业务枚举**默认 `enums/`；统一事务边界与会话生命周期 |
| **interfaces** | HTTP、DTO、中间件；**endpoint 不写业务逻辑**；REST 资源路径与 CRUD 方法名（`store` / `show` / `update` / `destroy`）见 `fastapi-scaff-pipeline-db-router.mdc` |
| **infrastructure** | DB、缓存等技术实现；提供 Session 工厂/Provider 等技术能力 |
| **common** | 横切：统一响应、**仅跨模块通用**枚举、工具、异常 |

## Middleware 错误处理约束（新增）

- 中间件只负责鉴权、追踪、参数改写等横切逻辑，**不直接构造错误响应体**。
- 发生业务/系统异常时，中间件应仅 `raise` 对应异常（如 `BizException` / `SystemException`），由全局异常处理统一出参。

## 全项目异常处理约束（新增）

- 除 `ExceptionCaptureMiddleware` 统一异常出口外，项目内其他位置（Endpoint、Middleware、Application、Infrastructure、Common）禁止处理错误响应。
- 所有层只负责抛异常或上抛异常，不在本层吞异常并返回错误 JSON/Response。

## Session 使用约束（新增）

- Endpoint 不注入/传递 `AsyncSession`，避免“请求一进入就绑定会话”。
- Application 在用例内部按需获取 Session（Lazy Session），并负责 `commit/rollback` 策略。
- Infrastructure 提供 `engine/sessionmaker/provider`，不承载业务流程决策。

## 升级 DDD 的时机

复杂业务规则、多聚合、多子域协作时再加深 domain，当前阶段**不强制**全套 DDD，先保证分层清晰。
