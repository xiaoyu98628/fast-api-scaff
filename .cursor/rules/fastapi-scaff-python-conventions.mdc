---
description: Python 约定：__init__.py、import、函数长度、避免过度设计
globs: "**/*.py"
alwaysApply: false
---

# `__init__.py`

- **每个包目录**须有 `__init__.py`；默认**空文件**，不做导出。
- **禁止**在 `__init__.py` 中做 re-export（包含 `from .x import Y` 与 `__all__`）。

```python
# ❌ 禁止
from .codec import DataCodec
from .json import ApiResponse
from .xxx import *
__all__ = ["DataCodec"]
```

原则：统一使用显式完整模块路径导入，避免包级导出带来的命名歧义与依赖不透明。

# import 习惯

优先显式路径，避免依赖隐式 re-export：

```python
# ✅ 推荐
from app.common.utils.codec import DataCodec

# ❌ 禁止（禁止依赖包级导出）
from app.common.utils import DataCodec
```

# 函数与方法长度

- 单个**函数或方法**（从 `def` 下一行到该块结束）**不得超过 80 行**。
- 接近或超过上限时：拆分为私有辅助函数、独立步骤函数或小型类；避免单函数内多层嵌套与「流程 + 细节」混写。
- 例外需有明确理由（如生成的协议解析、经评审的性能热点），并在同文件内用简短注释标明原因与范围。

# 不要过度设计

高层不依赖低层**实现细节**，但当前阶段以**分层清晰**为先，不必强行抽象接口泛滥。
