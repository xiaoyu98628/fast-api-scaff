# SERVICE_CODE 登记表

`SERVICE_CODE` 为三位数字（`000`～`999`），每个服务唯一，一旦分配不可修改。环境变量仍可写两位（如 `01`），加载时会规范为 `001`。

| SERVICE_CODE | 服务名 | 仓库/说明 | 状态 |
|---|---|---|---|
| 001 | fast-api-scaff | 当前脚手架默认占位 | 已占用 |
