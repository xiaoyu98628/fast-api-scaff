# FastAPI 脚手架对比与迁移评估

> 对比日期：2026-09-09
>
> 参照脚手架：`/Users/xiaoyu/www/python/company/template/python-fastapi-template`
>
> 当前脚手架：`/Users/xiaoyu/www/python/works/fast-api-scaff`

## 一、执行摘要

建议将当前脚手架作为需要持续迭代、多人协作或多进程入口的新项目基线。

这项建议的主要依据不是功能数量，而是当前脚手架把业务边界、依赖方向、事务、资源生命周期和质量检查固化成了可运行、可测试的工程约束。参照脚手架已经能够承载常规 FastAPI 业务，并非只有健康检查；它已有用户 CRUD、JWT 与 Redis 鉴权、文件上传、对象存储和 SSE 示例。当前脚手架的优势在于长期演进能力，而不是对参照脚手架所有功能的完全覆盖。

两者不是原地兼容升级关系。当前脚手架尚未提供参照脚手架已有的文件上传、本地/OSS/COS 存储和入站 SSE；用户字段、删除语义、认证机制、路由保护范围、配置名和数据库结构也不同。因此：

- 新建中长期项目可以优先采用当前脚手架；
- 只有少量接口、无需多宿主或复杂基础设施的项目，可以继续选择参照脚手架以降低认知和维护成本；
- 已使用参照脚手架的项目，应先完成契约和能力盘点，再决定是否迁移，不能直接替换目录或配置。

## 二、对比基线与验证依据

本次对比以两个仓库的受版本控制内容为准，不把未实现的规划能力当成现有能力。

| 项目 | Git 基线 | 项目版本 | 测试与 CI |
| --- | --- | --- | --- |
| 参照脚手架 | `19e9aeb07984c3f0d528b90a07fcb0af51a92787` | `0.1.0` | 没有受版本控制的测试文件，未发现 GitHub Actions 工作流 |
| 当前脚手架 | `745d281545ae5a98422bacc8821da0ae3997f038` | `1.0.0` | 63 个 `test_*.py` 文件；本地验证 420 项通过；包含 GitHub Actions |

当前脚手架的本地测试命令和结果：

```text
uv run --frozen python -m pytest -q
420 passed in 8.04s
```

当前 CI 还会执行 Ruff、格式检查、ty、`git diff --check`，并使用临时 MySQL 与 PostgreSQL 服务验证 migration 的 upgrade、downgrade 和再次 upgrade。

参照仓库当前存在未跟踪的 `.DS_Store` 和 `storage/logs/`，它们属于本地运行或系统文件，未纳入能力比较。参照仓库没有可执行的项目测试套件，因此不能用历史测试数量替代当前验证结果。

测试数量只能说明回归保护的规模，不能单独证明设计质量。后续结论同时依据目录结构、依赖方向、运行入口、资源管理、公开契约和文档。

## 三、能力对比

| 维度 | 参照脚手架 | 当前脚手架 | 评估 |
| --- | --- | --- | --- |
| 项目定位 | 轻量、面向 HTTP 的分层 FastAPI 模板 | 面向长期演进的模块化单体脚手架 | 当前脚手架治理能力更强，参照脚手架上手成本更低 |
| 业务组织 | 全局 `services/repositories/models/schemas` 技术分层 | 限界上下文内 Domain/Application/Infrastructure 分层 | 当前结构更适合业务模块持续增长 |
| 架构保护 | README 与 `.cursor/rules` 约定，依赖人工审查 | AST 架构测试约束依赖方向和上下文边界 | 当前脚手架可自动发现架构回退 |
| HTTP 入口 | FastAPI、统一响应、错误码、中间件、OpenAPI、用户、文件和 SSE 路由 | FastAPI、统一响应、业务码、分页、认证、用户路由和中间件 | 两者都有完整 HTTP 样例，但具体契约不同 |
| Console | 无 | Typer Console，共享应用用例与稳定退出码 | 当前脚手架适合一次性任务和运维命令 |
| Worker/队列 | 无 | Redis Streams、Kafka、RabbitMQ、独立 Worker、失败存储与重放 | 当前脚手架覆盖后台任务入口 |
| 用户样例 | 用户 CRUD、昵称、状态、密码更新和软删除 | 用户聚合、邮箱、状态、密码重置和物理删除 | 都有业务闭环，但数据模型和行为不能直接互换 |
| 认证 | JWT 签名，Redis 保存 Token 存在性，全局中间件保护业务路由 | 随机会话 Token，只保存摘要到 SQL；`/api/v1/auth/me` 和退出校验会话 | 当前实现减少 Token 明文存储，但不是 JWT 兼容实现 |
| 数据库 | 单一 MySQL 配置，应用使用 aiomysql | MySQL、PostgreSQL、SQLite 命名连接，使用 asyncmy/asyncpg/aiosqlite | 当前脚手架环境适配和扩展能力更强 |
| 数据访问 | Service 管事务，Repository 操作 ORM；业务层会构造 HTTP 响应 | Domain Repository 协议、Mapper、UoW 和基础设施实现 | 当前脚手架的业务与 ORM/HTTP 边界更清晰 |
| Migration | 两个用户表相关版本 | 用户、会话和失败任务表三个版本 | 两边都有真实 migration，表结构不同 |
| 缓存 | Redis，主要用于访问令牌 | Redis、Memcached，统一字节级 KV 接口 | 当前脚手架支持命名连接和驱动切换 |
| 出站 HTTP | 全局共享 httpx `AsyncClient`，支持普通与流式请求 | 受 Manager 管理的普通/流式独立池、阶段超时、大小限制、诊断和日志 | 当前脚手架对并发、流生命周期和失败边界控制更完整 |
| 文件存储 | 文件上传；Local、阿里云 OSS、腾讯云 COS | 无现成文件上传或对象存储模块 | 这是迁移前必须补齐或明确放弃的反向差距 |
| SSE | 入站 SSE 响应、心跳、错误事件和上游 SSE 代理示例 | 有出站流式 HTTP，但没有入站 SSE 封装和路由 | 出站流支持不能等同于 SSE 业务能力 |
| 资源生命周期 | 数据库、Redis、HTTP Client 使用模块级延迟单例，由 HTTP lifespan 统一关闭 | Container + Runtime 管理延迟资源、失败清理和逆序关闭，供 HTTP/Console/Worker 共用 | 当前脚手架更适合多宿主和复杂清理场景 |
| 配置 | `DB_*`、`REDIS_*`、`JWT_*`、`STORAGE_*` 等扁平配置 | 数据库、缓存和队列使用可扩展的命名连接配置 | 当前配置扩展能力更强，但不能直接复用原环境变量 |
| 日志 | 控制台与轮转文件通道、Trace ID | JSON/Text、Request ID、访问日志、数据库查询日志、可注册 Handler | 能力侧重点和字段契约不同 |
| 测试 | 仓库内没有测试套件 | 覆盖 Domain、Application、持久化、HTTP、Console、Worker、Manager、并发、取消和架构 | 当前脚手架回归保护明显更完整 |
| CI | 未发现工作流 | pytest、Ruff、格式、ty 和多数据库 migration 验证 | 当前脚手架能在合并前自动守住质量基线 |
| 文档 | README 和 5 份 Cursor 规则 | README 和 15 篇专题文档 | 当前脚手架更适合团队 onboarding 和长期维护 |

## 四、架构与维护取舍

### 1. 参照脚手架的价值与边界

参照脚手架采用容易理解的调用链：

```text
api/controllers -> services -> repositories / core
```

它已经提供真实业务闭环：

- 用户创建、查询、更新、状态修改和软删除；
- 用户登录、JWT 签发和 Redis Token 校验；
- SQLAlchemy Repository 与 Service 事务提交；
- 用户表 Alembic migration；
- 本地、OSS、COS 文件存储；
- SSE 事件、心跳和上游流代理；
- 统一响应、错误码、请求日志、Trace ID 和 OpenAPI 鉴权描述。

这种按技术职责划分的结构适合规模较小、以 HTTP 为唯一入口的项目。主要边界在于：

- 业务增加后，全局 Service、Repository 和 Model 容易成为跨业务共享区；
- Service 直接使用 HTTP Response DTO 和统一响应，业务编排与 HTTP 表达存在耦合；
- 数据库、Redis 和 HTTP Client 依赖模块级全局状态；
- 资源生命周期只在 HTTP 入口组装，新增 Console 或 Worker 时需要重新设计；
- 架构规则主要依赖文档、Cursor 规则和 Code Review，没有自动化架构测试；
- 仓库内没有测试套件和 CI，变更后的行为缺少可重复验证。

参照脚手架并不会在应用启动时主动探测数据库或 Redis；相关资源在首次调用时创建，HTTP lifespan 负责关闭。数据库和 Redis 健康端点会在被请求时执行连接检查。

### 2. 当前脚手架的主要收益

当前脚手架把业务按限界上下文组织，并在上下文内划分 Domain、Application 和 Infrastructure：

```text
HTTP / Console / Worker -> Interface -> Application -> Domain
                                 ^
                                 |
               Composition Root + Infrastructure

Runtime -> Container / Manager 生命周期
```

主要收益包括：

- Domain 保存业务对象、值对象和不变量，不依赖 FastAPI 或 SQLAlchemy；
- Application 通过窄协议编排用例、事务、密码哈希和会话；
- Infrastructure 实现 Repository、UoW、Mapper 和各类技术驱动；
- Interface 只负责 HTTP、Console 或 Worker 的协议转换；
- Composition Root 选择具体实现并完成依赖注入；
- 架构测试自动阻止依赖反转、上下文穿透和驱动泄漏；
- ApplicationRuntime 为多个宿主统一处理启动、失败清理和关闭；
- Database、Cache、HTTP 和 Queue Manager 统一延迟初始化和资源所有权；
- 测试覆盖成功路径、异常路径、并发、取消、生命周期和公开契约。

这些能力降低了多人协作时目录、事务和生命周期逐渐分叉的风险，也让新增业务上下文有一条可复制、可验证的实现路径。

### 3. 当前脚手架的成本与边界

当前脚手架的结构和依赖更多，团队需要理解限界上下文、应用端口、Mapper、UoW、Composition Root 和 Runtime 等边界。对于只有少量接口、生命周期简单的服务，这些层次可能带来高于收益的认知和代码成本。

当前仓库提供的是工程基线，不是完整业务平台。它没有现成的文件存储、文件上传和入站 SSE，也没有刷新令牌、Scheduler、领域事件、Outbox、Saga 或跨数据库原子事务。后面这些能力应在真实需求出现后设计，不能写成当前已有功能。

当前用户上下文是架构样例：用户管理接口默认公开，认证只演示登录、当前用户和退出；它不能直接等同于项目最终的账号或权限系统。

## 五、迁移中的非兼容项

### 1. 用户数据与接口

两套用户样例的字段和行为不同：

| 项目 | 参照脚手架 | 当前脚手架 |
| --- | --- | --- |
| ID | 26 位十六进制字符串 | UUID |
| 基本资料 | username、nickname | username、email |
| 更新行为 | 可更新 nickname 和 password | 更新 username 和 email；密码使用独立重置接口 |
| 删除 | `deleted_at` 软删除 | 物理删除 |
| 时间字段 | ORM 包含创建、更新和删除时间，对外用户结构不全部返回 | 对外返回 created_at、updated_at |
| 分页 | `page/page_size`，返回 `data + meta` | `page/limit`，返回 `items + meta` |

迁移前必须明确字段映射、ID 转换、历史软删除数据、唯一约束、分页响应和客户端兼容方式。不能直接用当前 migration 覆盖原表。

### 2. 认证

参照脚手架使用 JWT，并在 Redis 中保存 Token 存在性；除健康检查、登录和文档外，鉴权中间件默认保护所有路由。当前脚手架使用不可解析的随机 Token，只在 SQL 中保存摘要，认证依赖在需要登录态的端点显式注入，而用户 CRUD 当前是公开样例。

迁移时需要决定：

- 是否保留现有 JWT 契约；
- 已签发 Token 是否需要兼容期；
- Redis Token 数据如何处理；
- 哪些路由必须登录；
- 登录失败的状态码和错误语义是否保持；
- 客户端是否依赖 JWT claims。

### 3. 文件上传、对象存储与 SSE

参照脚手架已有下列能力，当前脚手架没有等价模块：

- `multipart/form-data` 文件上传；
- 文件类型和大小策略；
- Local、OSS、COS 存储驱动；
- 本地静态文件挂载；
- SSE 心跳、结束事件、流内错误和上游代理。

迁移前应逐项决定“迁入、重新设计或明确停用”。当前脚手架的出站流式 HTTP 只能作为实现上游拉流的基础，不能替代面向客户端的 SSE 协议封装。

### 4. 配置与运行契约

主要配置变化包括：

```text
DB_HOST / DB_PORT / DB_DATABASE / ... -> DB_CONNECTIONS__MAIN__*
REDIS_*                                -> CACHE_CONNECTIONS__<NAME>__*
JWT_*                                  -> AUTH_SESSION_TTL_SECONDS
X-Trace-Id                             -> X-Request-ID
```

`STORAGE_*` 没有当前脚手架的直接映射。迁移时需要同步检查环境配置、密钥管理、Compose、启动命令、Header 和依赖这些名称的调用方。

### 5. 依赖与运行行为

关键依赖和实现会发生变化：

- aiomysql -> asyncmy，并增加 asyncpg、aiosqlite；
- bcrypt -> pwdlib/Argon2；
- httpx -> httpx2；
- JWT + Redis Token -> SQL 会话摘要；
- 单一 Redis -> Redis/Memcached 命名缓存；
- 增加 Typer、aio-pika 和 aiokafka；
- 当前脚手架不再依赖 python-multipart、OSS2 和 COS SDK。

数据库 URL、密码哈希兼容、HTTP 超时与流行为、Redis 序列化、连接池参数和容器构建结果都需要分别验证。

## 六、建议采用方式

### 1. 新项目

出现以下任一情况时，建议使用当前脚手架：

- 业务模块会持续增加；
- 需要明确领域、事务和持久化边界；
- HTTP 之外还需要 Console 或 Worker；
- 需要 MySQL、PostgreSQL、SQLite 多环境适配；
- 需要 Redis、Memcached 或多种消息队列；
- 多人协作时需要自动保护架构和质量基线；
- 脚手架将作为多个项目共享的长期标准。

如果项目只有少量 HTTP 接口、生命周期简单，并且文件存储或 SSE 是主要能力，参照脚手架可能更直接。是否采用当前脚手架应由预期复杂度决定，而不是由目录或功能数量决定。

### 2. 已有项目

建议按业务和契约逐步迁移：

1. 固定现有 URL、method、状态码、响应信封、分页和 Header；
2. 盘点用户表、软删除数据、JWT、Redis Token、文件和 SSE 使用情况；
3. 明确保留、兼容、替换和停用清单；
4. 按业务上下文建立 Domain/Application，并先补用例测试；
5. 实现 Mapper、Repository、UoW 和对应 migration；
6. 接入 HTTP Interface，必要时增加临时兼容层；
7. 按范围补齐文件存储、SSE、Console 或 Worker；
8. 迁移环境变量、依赖和容器配置；
9. 先运行受影响测试，再执行 pytest、Ruff、格式、ty、`git diff --check` 和 migration 往返验证。

不建议把参照项目代码整体复制到当前目录后再集中重构。按上下文和公开契约分批迁移，更容易定位问题并保留回退空间。

## 七、已发现的额外风险

参照脚手架的 `app/services/sse.py` 包含硬编码的 Dify Bearer 凭据样式文本。无论它当前是否仍有效，都应按潜在密钥处理：确认用途、及时轮换、移出代码，并检查是否需要清理 Git 历史。对比文档和迁移代码不得复制该值。

## 八、最终建议

当前脚手架更适合作为中长期项目和团队协作的工程基线，因为它对模块边界、事务、资源生命周期、多宿主和质量检查提供了系统性保障。

这不意味着参照脚手架没有真实业务能力，也不意味着切换可以自动获得所有原有功能。文件存储、SSE、JWT 契约、用户数据模型和路由保护策略都是明确的迁移工作。只有在这些差异被盘点、取舍并验证后，切换结论才可以落地。

因此，建议采用“新项目优先使用当前脚手架，已有项目经过兼容评估后分阶段迁移”的策略，而不是把当前脚手架描述为参照脚手架的直接替代品。
