# FastAPI 脚手架切换汇报

> 对比日期：2026-09-08
>
> 原脚手架：`python-fastapi-template` 0.1.0
>
> 当前脚手架：`fast-api-scaff` 3.0.7

## 一、汇报结论

建议后续项目由原脚手架切换到当前脚手架。

这次切换并不是因为原脚手架无法运行，而是因为两者解决的问题已经不同：原脚手架适合作为轻量 FastAPI 服务的起点，当前脚手架则面向需要持续迭代、多人协作和多入口运行的模块化单体项目。

原脚手架已经提供 HTTP、配置、统一响应、数据库、缓存和日志等基础能力，但业务分层主要停留在目录约定，缺少可运行的完整业务样例、依赖方向保护和统一的跨宿主生命周期。随着业务增长，团队仍需自行补齐领域边界、事务、认证、后台任务、命令行工具、测试体系和配套文档，容易在不同项目中形成不一致的实现。

当前脚手架在保留通用基础设施的基础上，进一步提供了：

- 可执行的模块化单体分层和依赖约束；
- HTTP、Console、Worker 三类宿主共享的组合根与资源生命周期；
- 完整的用户业务、事务、认证和数据库迁移样例；
- 多数据库、多缓存、出站 HTTP、队列与失败任务处理能力；
- 更完整的自动化测试、静态检查、CI 和专题文档。

切换后的核心收益不是“功能数量更多”，而是把项目长期演进中最容易分叉或出错的架构边界、资源生命周期、事务和质量标准固化为可复用、可验证的工程基线。

## 二、对比范围与依据

本次对比基于两个仓库当前实际代码、配置、依赖、测试和文档，不把规划中的能力当成已实现能力。

主要检查范围：

- 项目目录和模块职责；
- HTTP 应用的装配与生命周期；
- Domain、Application、Infrastructure 和 Interface 的依赖方向；
- 数据库、缓存、出站 HTTP、日志和队列能力；
- 配置结构、容器和本地开发方式；
- 测试、Lint、格式、类型检查和 CI；
- README、专题文档和业务示例。

本地验证结果：

| 项目 | 测试文件 | 测试结果 | CI |
| --- | ---: | ---: | --- |
| 原脚手架 | 7 个 | 30 项通过，耗时约 2.5 秒 | 仓库内未发现 GitHub Actions 工作流 |
| 当前脚手架 | 63 个 | 420 项通过，耗时约 10.9 秒 | 包含测试、Ruff、格式、ty，以及 MySQL/PostgreSQL 迁移往返验证 |

测试数量只能反映当前覆盖规模，不能单独代表代码质量；但结合测试分层和 CI 内容，可以说明当前脚手架对架构、生命周期、异常路径和公开契约提供了更强的回归保护。

## 三、原脚手架的主要痛点

### 1. 业务架构主要依赖约定，缺少自动保护

原脚手架推荐的依赖方向是：

```text
api/controllers -> services -> repositories / core
```

这种结构容易理解，适合小型项目，但 `services`、`repositories`、`models` 是按技术职责划分的全局目录。业务增加后，不同业务模块容易共同依赖这些目录，逐渐形成“大 Service”“通用 Repository”和跨模块直接调用。

同时，原脚手架没有架构测试来限制导入方向。Controller 是否绕过 Service、Service 是否直接依赖 FastAPI 或具体数据库实现，主要依赖人工 Code Review 发现。

### 2. 缺少完整、可复制的业务闭环

原脚手架的 HTTP 路由主要是 `/health` 和 `/api/v1/health`，数据库 Alembic migration 也没有业务版本。虽然提供了数据库和缓存基础封装，但没有展示一个真实业务从请求到持久化的完整链路。

新项目仍需自行决定并实现：

- 领域对象和业务校验应该放在哪里；
- Application Service 如何组织用例；
- Repository 如何隔离 ORM；
- 事务由谁提交和回滚；
- ORM Model 与业务对象如何转换；
- 业务错误如何稳定映射成 HTTP 响应；
- HTTP 之外的入口如何复用同一业务逻辑。

这些决策如果由各项目分别完成，最终会产生不同的目录、事务习惯和异常处理方式，增加人员切换和代码审查成本。

### 3. 应用装配集中在 HTTP 入口，难以复用到其他宿主

原脚手架在 `app/main.py` 中直接完成数据库、缓存、HTTP Client、中间件和路由的启动与关闭。对于只有 HTTP API 的服务，这种方式足够直接；但增加命令行任务、独立 Worker 或其他进程入口时，容易复制一套初始化和清理逻辑。

一旦多个入口分别维护生命周期，常见风险包括：

- 某个入口漏初始化依赖；
- 不同入口关闭资源的顺序不一致；
- 启动中途失败时只清理了部分资源；
- HTTP 与后台任务使用了不同的业务装配方式；
- 测试需要依赖或修改模块级全局状态。

### 4. 基础设施能力的扩展空间有限

原脚手架当前实现以以下范围为主：

- 数据库：MySQL 命名连接；
- 缓存：Redis、Memcached；
- 出站 HTTP：共享 `httpx.AsyncClient`；
- 进程入口：FastAPI HTTP；
- 异步事件：SSE 响应封装。

当项目需要 PostgreSQL、SQLite 本地开发、独立连接池、后台队列或 Worker 时，需要在现有结构上另行设计配置、驱动注册、生命周期和测试边界。

### 5. 数据库和缓存的启动策略偏向集中启停

原脚手架通过 `DB_ENABLED`、`CACHE_ENABLED` 控制能力开关。启用后，HTTP lifespan 负责集中启动；配置还支持在启动阶段检查数据库连接，缓存启动也会执行连接检查。

这种方式的优点是问题能在启动时暴露，但也把 HTTP 应用启动与外部基础设施绑定得更紧。对于只访问健康检查、只使用部分命名连接或希望本地先使用轻量存储的场景，灵活性不足。

### 6. 配置扩展需要修改 Python 组装代码

原脚手架的默认数据库环境变量采用扁平的 `DB_HOST`、`DB_PORT` 等形式。增加第二个命名连接时，需要在 Python 配置代码中创建新的 `MysqlConfig` 并为其指定另一组环境变量前缀。

这种设计在连接数量少时很清楚，但当数据库、缓存或驱动类型增多时，配置结构与代码组装会逐渐耦合，环境之间增加或调整连接也更容易要求代码变更。

### 7. 工程质量闭环较轻

原脚手架配置了 pytest、Ruff、Black 和 ty，但仓库内未发现 CI 工作流；测试集中在 HTTP 基础行为、响应、异常、SSE、HTTP Client、数据库和缓存，共 30 项。

目前缺少对以下内容的自动回归保护：

- 分层依赖方向；
- 完整业务用例与事务；
- 多宿主生命周期；
- 数据库 migration 的实际 upgrade/downgrade；
- 多驱动一致性；
- 并发初始化、取消和资源关闭异常。

### 8. 文档集中在 README，复杂能力不易长期维护

原脚手架主要通过 README 说明启动、目录、缓存、数据库和 SSE。随着配置、业务模块和运行入口增加，单一 README 容易变得过长，也难以让代码、配置示例、生命周期约束和故障处理保持同步。

## 四、当前脚手架的主要优点

### 1. 从“目录约定”升级为“可验证的模块化单体”

当前脚手架按限界上下文组织业务，并在上下文内部划分：

```text
Domain -> Application -> Infrastructure
```

实际依赖方向是由外向内：

```text
入口 -> Bootstrap / Composition -> Interface -> Application -> Domain
                              \-> Infrastructure
```

其中：

- Domain 保存业务对象、不变量和值对象，不依赖 FastAPI、ORM 或全局容器；
- Application 编排用例和事务协议，只依赖当前上下文的 Domain/Application；
- Infrastructure 实现 Repository、Unit of Work、Mapper 和技术适配；
- Interface 负责 HTTP、Console、Worker 的协议转换；
- Bootstrap/Composition 负责选择具体实现并完成依赖注入。

`tests/test_architecture.py` 会扫描导入并阻止依赖反转、上下文穿透、Interface 直接依赖持久化实现、共享 Infrastructure 反向依赖业务等问题。架构不再只依靠口头约定。

### 2. 提供真实业务闭环，而不只是空目录

当前脚手架内置用户限界上下文，完整展示：

- User 聚合、值对象、状态和领域错误；
- Application Service、Command、DTO；
- Repository 和 Unit of Work 协议；
- SQLAlchemy Model、Mapper、Repository、UoW；
- 用户 CRUD、状态修改、密码重置；
- 登录、当前用户、退出和数据库会话；
- HTTP 与 Console 复用同一应用用例；
- Alembic 用户、会话和失败任务表 migration；
- Domain、Application、Infrastructure、HTTP 和 Console 分层测试。

新项目可以沿着现有链路复制新的上下文，减少对“业务代码到底应该放在哪里”的重复讨论。

### 3. 统一多宿主的装配与资源生命周期

当前脚手架提供三个独立入口：

- `app.main`：HTTP/ASGI；
- `app.console`：Typer Console；
- `app.worker`：独立队列 Worker。

三个入口共同使用：

- `ApplicationContainer` 保存应用依赖；
- `build_application_container()` 完成全局装配；
- `ApplicationRuntime` 统一启动、失败清理和关闭；
- HTTP、Console、Worker 各自只处理宿主特有的日志、参数和运行方式。

资源关闭采用逆序执行和错误聚合，并在不可取消区域内尽量完成全部清理。数据库、缓存、出站 HTTP 和队列管理器都遵守同一生命周期，降低多入口行为分叉的风险。

### 4. 基础设施通过 Manager、Provider 和 Driver 解耦

当前脚手架不要求业务层直接依赖 Redis、SQLAlchemy、HTTPX2 或消息队列客户端。通用技术能力由 Manager 管理，Provider 把配置转换为资源定义，具体 Driver 被限制在基础设施边界内。

已实现能力包括：

| 能力 | 当前实现 |
| --- | --- |
| 数据库 | MySQL、PostgreSQL、SQLite 异步 SQLAlchemy |
| 缓存 | Redis、Memcached 字节级 KV |
| 出站 HTTP | 普通与流式请求、独立连接池、阶段超时、池压力诊断、响应大小限制、结构化日志 |
| 队列 | Redis Streams、Kafka、RabbitMQ |
| Worker | 动态解析 `QueueJob`、并发消费、投递内重试、失败任务 SQL 存储和 Console 重放 |
| 日志 | JSON/Text、Request ID、访问日志、数据库查询日志、可注册 Handler |

这使后续扩展更多业务上下文时，可以复用技术能力而不让业务层依赖具体驱动。

### 5. 延迟初始化更适合按需使用

当前脚手架在构建应用容器时不会主动访问数据库、缓存、HTTP 上游或队列。资源在首次实际使用时创建，并在应用结束时统一释放。

主要收益：

- `/health` 不依赖数据库或远程缓存即可响应；
- 未使用的数据库连接不会产生网络访问；
- 本地开发可使用 SQLite，同时让缓存保持与部署环境相同的 Redis/Memcached 驱动；
- 单元测试可以注入 Provider 或 Fake，避免依赖真实外部服务；
- HTTP、Console 和 Worker 可以只使用各自需要的资源。

### 6. 配置结构可以通过环境直接扩展命名连接

当前脚手架采用嵌套环境变量，例如：

```dotenv
DB_CONNECTIONS__MAIN__DRIVER=mysql
DB_CONNECTIONS__LEGACY__DRIVER=postgresql
CACHE_CONNECTIONS__SESSION__DRIVER=redis
CACHE_CONNECTIONS__LOCAL__DRIVER=memory
QUEUE_CONNECTIONS__RABBITMQ__DRIVER=rabbitmq
```

新增命名连接通常只需调整环境配置，不需要为每个环境修改 Python 组装代码。不同 Driver 会进行严格字段校验，拼错字段或携带不支持的参数会在明确阶段报错。

配置对象加载后不可变，并在进程内缓存，减少不同组件读取到不一致环境状态的风险。

### 7. HTTP 契约和异常处理更加完整

当前 HTTP 层集中处理：

- 统一 JSON 响应和分段业务码；
- Request ID 生成、透传和非法值处理；
- 请求参数校验、业务错误和未知异常映射；
- CORS 与错误响应的一致性；
- 访问日志与敏感信息边界；
- 分页请求和响应协议；
- OpenAPI 响应模型和错误说明。

Interface 只负责协议转换，业务规则仍由 Application/Domain 处理，因此相同用例可以被 Console 等其他入口复用。

### 8. 测试从“功能点验证”扩展到“架构与生命周期保护”

当前脚手架的测试不仅验证成功路径，还覆盖：

- Domain 不变量和值对象；
- Application 用例顺序、提交和失败路径；
- Mapper、Repository、UoW 和唯一约束异常；
- Manager 延迟初始化、并发获取和关闭；
- HTTP 中间件、异常映射、分页、OpenAPI 和 CORS；
- Console 参数、输出、错误流和退出码；
- Worker 解析、执行、并发、取消和关闭；
- 出站 HTTP 流生命周期、取消和连接池；
- 日志字段与敏感数据隐藏；
- 架构依赖方向；
- SQLite migration，以及 CI 中的 MySQL/PostgreSQL migration 往返。

这类测试更接近长期维护中的真实风险，而不只是证明接口当前能够返回成功。

### 9. 文档与实现形成配套手册

当前脚手架除 README 外，还提供以下专题文档：

- 快速开始与配置参考；
- 架构和开发质量规范；
- HTTP、认证与 Console；
- 数据库、缓存和出站 HTTP；
- 队列、Worker 和日志；
- 故障排查。

文档同时说明能力边界、生命周期、错误语义和验证方式，更适合团队 onboarding、Code Review 和问题排查。

## 五、新旧脚手架能力对比

| 维度 | 原脚手架 | 当前脚手架 | 切换价值 |
| --- | --- | --- | --- |
| 项目定位 | 最小 FastAPI 服务模板 | 可持续演进的模块化单体脚手架 | 从“能启动”升级为“可长期维护” |
| 业务组织 | 全局 services/repositories/models | 限界上下文内 Domain/Application/Infrastructure | 降低跨业务耦合 |
| 架构保护 | README 约定 | AST 架构测试自动校验 | 防止依赖方向逐步腐化 |
| HTTP | 健康检查、统一响应、异常、SSE | 统一响应、业务码、分页、认证、完整业务路由、中间件 | 提供可复制的接口实现标准 |
| Console | 无 | Typer 命令及稳定退出码 | 运维和一次性任务复用应用用例 |
| Worker/队列 | 无 | Redis/Kafka/RabbitMQ、Worker、失败存储与重放 | 后台任务具备统一入口和生命周期 |
| 数据库 | MySQL 命名连接 | MySQL/PostgreSQL/SQLite 命名连接 | 适应更多环境和业务场景 |
| 数据访问 | Manager/Facade，业务样例缺失 | Repository/UoW/Mapper 与完整上下文样例 | 明确事务和 ORM 边界 |
| 缓存 | Redis/Memcached，JSON 仓储 | Redis/Memcached，Driver 无关字节 KV | 环境行为更一致，基础边界更清晰 |
| 出站 HTTP | 共享 httpx Client | 普通/流式独立池、超时、限制、诊断与日志 | 提升高并发和流式场景可控性 |
| 资源生命周期 | 由 HTTP main.py 集中管理 | Container + Runtime，被多宿主共享 | 减少重复装配和清理遗漏 |
| 业务样例 | 版本化健康检查 | 用户 CRUD、事务、密码、会话认证 | 新业务有完整参考实现 |
| Migration | 空 migration head | 用户、会话、失败任务表 migration | 展示模型到数据库的完整闭环 |
| 测试规模 | 7 个文件，30 项通过 | 63 个文件，420 项通过 | 回归保护覆盖更多层次和异常路径 |
| CI | 未发现仓库内工作流 | 全量质量检查和多数据库 migration 验证 | 合并前自动守住工程基线 |
| 文档 | 主要集中在 README | README + 15 篇专题文档 | 降低学习和维护成本 |

## 六、当前脚手架目录说明

### 1. 根目录

```text
fast-api-scaff/
├── app/                    应用代码
├── database/               Alembic 环境、模型注册和 migration
├── docs/                   使用、架构和专题文档
├── tests/                  按能力和分层组织的自动化测试
├── storage/                本地数据与日志目录
├── .github/workflows/      CI 质量检查
├── sample.env              可复制的环境变量模板
├── compose.yml             HTTP 与 Worker 的本地容器编排
├── Dockerfile              多阶段、非 root 运行镜像
├── pyproject.toml          项目元数据、依赖和工具配置
├── uv.lock                 可复现依赖锁文件
└── README.md               项目入口说明
```

### 2. `app/` 应用目录

```text
app/
├── main.py                 HTTP/ASGI 薄入口
├── console.py              Console 薄入口
├── worker.py               Worker 薄入口
│
├── bootstrap/              组合根和各宿主的启动装配
│   ├── build.py            构建 ApplicationContainer
│   ├── http/               FastAPI 创建、日志和 lifespan
│   ├── console/            Console 应用、日志和生命周期
│   └── worker/             Worker 应用、日志和生命周期
│
├── config/                 Pydantic Settings 配置模型
│   ├── app.py              应用名称、版本、环境和服务码
│   ├── auth.py             会话有效期
│   ├── cache.py            多缓存连接
│   ├── cors.py             跨域配置
│   ├── database.py         多数据库连接
│   ├── http.py             出站 HTTP 超时与连接池
│   ├── logging.py          日志 Handler 与格式
│   ├── queue.py            队列与 Worker
│   └── settings.py         聚合并缓存完整配置
│
├── contexts/               按业务限界上下文组织代码
│   └── user/
│       ├── domain/         聚合、值对象、领域错误和 Repository 协议
│       ├── application/    用例、Command/DTO、UoW 和安全协议
│       ├── infrastructure/ ORM、Mapper、Repository、UoW 和密码适配
│       ├── jobs/           用户上下文相关后台任务
│       └── composition.py  用户上下文内部装配
│
├── infrastructure/        跨上下文共享的技术基础设施
│   ├── database/           DatabaseManager、Provider、连接和日志
│   ├── cache/              CacheManager、Client、Provider 和连接
│   ├── http/               HttpClientManager、请求/流与具体 Driver
│   ├── queue/              QueueManager、协议、Driver、失败存储
│   └── logging/            日志配置、格式化、上下文和 Driver
│
├── interfaces/             入站协议适配器
│   ├── http/               路由、Controller、Schema、中间件、统一响应和异常映射
│   ├── console/            命令发现、参数、展示和退出码
│   └── worker/             Job 解析、执行、消费并发和 Runner
│
└── runtime/                宿主无关的容器、生命周期和路径约定
```

### 3. `database/` 数据库目录

```text
database/
└── main/
    ├── alembic.ini         main 数据库 Alembic 配置
    ├── model_registry.py   显式注册 ORM metadata
    └── migrations/
        ├── env.py          migration 运行环境
        └── versions/       用户、会话和失败任务表版本
```

数据库目录按 migration stream 组织。当前只有 `main`；未来如果不同数据库拥有完全独立的表结构，可以增加独立 stream，而不是让所有数据库强制共用一套版本。

### 4. `tests/` 测试目录

```text
tests/
├── test_architecture.py    依赖方向和驱动边界
├── bootstrap/              Runtime 与 lifespan
├── users/                  Domain、Application、持久化、HTTP 和认证
├── http/                   响应、异常、中间件、CORS、分页和 OpenAPI
├── console/                命令、展示、日志和生命周期
├── worker/                 Job 解析、执行和关闭
├── database/               配置、Provider、Manager、连接和 migration
├── cache/                  配置、Provider、Client、Manager 和 codec
├── outbound_http/          请求、流、连接池、取消和日志
├── queue/                  消息、Driver、策略和失败存储
└── logging/                日志配置、Formatter 和 Driver
```

这种结构让问题可以先在对应层快速定位，再通过全量检查确认没有跨模块回归。

## 七、切换时需要正视的成本与风险

当前脚手架不是原脚手架的原地兼容升级，切换应被视为一次应用骨架迁移。

### 1. 目录和导入路径需要迁移

原有的 `app/api`、`app/core`、`app/services`、`app/repositories`、`app/models` 不应直接整体复制到新目录。需要先识别每段代码的真实职责，再分别放入：

- `contexts/<业务>/domain`；
- `contexts/<业务>/application`；
- `contexts/<业务>/infrastructure`；
- `interfaces/http`；
- 顶层共享 `infrastructure`；
- `bootstrap` 或上下文 `composition`。

如果只做机械搬运，会保留旧耦合，同时失去新架构的价值。

### 2. 配置项不能直接沿用

数据库和缓存由扁平配置改为命名连接的嵌套配置，部分示例：

```text
DB_CONNECTION                  -> DB_DEFAULT
DB_HOST / DB_PORT / ...        -> DB_CONNECTIONS__MAIN__HOST / PORT / ...
CACHE_STORE                    -> CACHE_DEFAULT
CACHE_PREFIX                   -> CACHE_NAMESPACE + 连接 KEY_PREFIX
X-Trace-Id                     -> X-Request-ID
```

迁移时需要同步检查部署环境、`.env`、密钥平台、Compose、启动命令和调用方 Header，不能只替换代码。

### 3. 公共 HTTP 契约需要兼容评估

当前脚手架的响应封装、错误码、Request ID、分页和业务接口与原脚手架并非完全一致。已有客户端存在时，应逐项对比：

- URL 和 HTTP method；
- status code；
- 响应 envelope 和业务 code；
- 错误 message 与错误字段；
- Trace/Request ID Header；
- 分页参数和分页 metadata；
- SSE 需求是否需要从原项目迁入。

必要时应设置兼容期，而不是一次性改变所有外部契约。

### 4. 依赖和驱动发生变化

当前脚手架使用 `asyncmy`、`asyncpg`、`aiosqlite`、Redis 8、Memcachio、HTTPX2，以及队列相关依赖。原脚手架使用 `aiomysql`、`pymysql`、Redis 5、`python-binary-memcached` 和 `httpx`。

迁移时应重点验证：

- 数据库 URL 和连接池参数；
- Redis/Memcached 命令语义和序列化协议；
- HTTP 超时、重定向、代理和流式响应行为；
- 原有 migration 与当前 model registry 的兼容性；
- 锁文件和容器构建结果。

### 5. 当前脚手架仍有明确能力边界

当前脚手架已经覆盖通用工程基线，但并不等同于完整业务平台。现阶段没有把角色权限、刷新令牌、Scheduler、领域事件、Outbox、Saga、跨数据库原子事务、通用 HTTP 自动重试等包装成现有能力。

这些能力应在出现真实业务需求后按上下文边界设计。汇报时应强调“当前脚手架提供可靠扩展边界”，而不是承诺它已经解决所有未来需求。

### 6. 当前用户模块是架构样例，需要按项目裁剪

用户 CRUD、密码和会话认证展示了完整实现链路，但不能直接等同于每个项目最终的账号体系。新项目应明确哪些代码作为教学参考保留，哪些需要替换为真实业务规则，避免把样例接口无条件暴露到正式服务。

## 八、建议迁移方式

建议采用“新骨架承载、按业务上下文逐步迁移”的方式：

1. 先冻结原项目的公开接口、配置、数据库表和后台任务清单；
2. 在当前脚手架中建立对应业务上下文，而不是复制原目录；
3. 先迁移 Domain/Application 规则，并使用 Fake Repository 补齐用例测试；
4. 再迁移 ORM、Repository、UoW、Mapper 和 migration；
5. 接入 HTTP Interface，并验证旧客户端契约；
6. 按需要迁移缓存、出站 HTTP、Console 和 Worker；
7. 同步环境变量、容器配置和专题文档；
8. 运行针对性测试后，再执行全量 pytest、Ruff、格式、ty 和 migration 验证；
9. 在测试环境完成数据和流量验证后再切换入口。

不建议把旧代码一次性复制后再集中重构。按上下文分批迁移更容易控制兼容风险，也能让每一步都具备独立验证结果。

## 九、可直接用于汇报的表述

### 一句话版本

原脚手架解决的是 FastAPI 服务快速启动问题，当前脚手架进一步解决了业务增长后的分层、事务、多入口、基础设施复用和质量治理问题，因此切换是从“项目模板”升级为“团队工程基线”。

### 管理视角版本

切换后的主要价值是降低长期维护成本和项目间差异。团队不再为每个项目重复设计目录、事务、异常、后台任务和生命周期，架构规则还能通过测试和 CI 自动检查。短期需要承担一次目录、配置和接口兼容迁移，但可以换取更稳定的交付标准、更低的新人理解成本和更可控的后续扩展。

### 技术视角版本

当前脚手架通过限界上下文、Domain/Application/Infrastructure 分层、Composition Root、ApplicationContainer 和 ApplicationRuntime，把业务规则、协议入口与技术驱动分离；再通过架构测试、分层测试和 CI 保证这些边界不会在迭代中失效。它提供的是一条完整且可验证的实现路径，而不只是更多公共工具类。

## 十、最终建议

如果项目始终只有少量健康检查或简单转发接口，原脚手架更轻，继续使用也有合理性。

如果项目满足以下任一条件，切换到当前脚手架的收益会更加明确：

- 业务模块会持续增加；
- 需要明确事务和持久化边界；
- HTTP 之外还需要 Console 或 Worker；
- 需要 MySQL/PostgreSQL/SQLite 等多环境适配；
- 多人协作时需要统一目录、配置和异常规范；
- 希望通过测试和 CI 自动保护架构与生命周期；
- 需要把脚手架作为多个项目共享的长期基线。

综合当前两个仓库的实现状态，建议采用当前脚手架作为后续项目基线，并把切换作为一次有兼容清单、分阶段验证和回滚方案的正式迁移，而不是简单替换目录。
